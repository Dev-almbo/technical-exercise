"""Project settings.

Kedro reads this module to configure the project. Keeping the defaults is fine
for this project; add entries here only when you need to customize hooks,
the config loader, session store, etc.
"""
